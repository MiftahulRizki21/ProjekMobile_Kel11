<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjekMobile_Kel11"
        tools:targetApi="31">

        <!-- 🔹 SPLASH (SATU-SATUNYA LAUNCHER) -->
        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔹 AUTH -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false" />

        <!-- 🔹 USER -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <!-- 🔹 DOCTOR -->
        <activity
            android:name=".fragments.dokter.DoctorActivity"
            android:exported="false" />

        <!-- 🔹 ADMIN -->
        <activity
            android:name=".DashboardAdmin"
            android:exported="false" />

        <activity
            android:name=".ProfilAdminActivity"
            android:exported="false" />

        <!-- 🔹 OPTIONAL -->
        <activity
            android:name=".ui.prediction.PredictionFormActivity"
            android:exported="false" />
        <receiver
            android:name=".receiver.ReminderReceiver"
            android:exported="false" />

        <activity
            android:name=".ui.consultation.ChatActivity"
            android:exported="false" />

        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>

</manifest>
